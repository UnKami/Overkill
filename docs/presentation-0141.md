# Overkill 0.14.1 — Top Relic Choices

On GitHub: **UnKami/Overkill → Releases → 0.14.1 → Assets → OverkillSetup-0.14.1.exe**. The portable ZIP is available beside the installer. Extract all ZIP files before launching Overkill.exe.

- Three compact relic choices now sit at the top of the battle screen.
- Removed the shared panel background, heading and instruction strip; only the options remain during assembly.
- Each available option pulses with a gentle colored outline. Reduced-motion mode keeps a steady highlight.
- Replacement choices use the same compact top position; numbered clock destinations still pulse. Keep & Sweep remains available separately.
- Clocks, health and the central battle view remain clear. Combat notices moved away from the choices.
- No changes to the nine-hour combat rules or twelve-relic starter deck. Existing 0.14 runs remain compatible.

Validation: rendered layout checks at 1080p, large-text 720p and ultrawide, including choice/button bounds, unobstructed clocks, preview stability, inspection, locked slots and empty reserves. Exported payload checks are recorded in the technical handoff.

This remains a playtest. Boss balance needs human playtesting. The installer is unsigned; the interactive installation wizard has not been tested. The existing nonfatal certificate-store and headless shutdown warnings remain.

Source tag: `v0.14.1-test`; feature branch: `feat/yonatan-top-relic-choices`. Gameplay awaits review before merging to main.
